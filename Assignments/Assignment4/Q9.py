#Create a tuple containing ONE item that is a list of three elements 'a', 'b', and 'c'.
#Verify that the length of the tuple is actually 1 by using the len() function.

minilist = ["a","b","c"]
minituple = (minilist,)
print(len(minituple))
